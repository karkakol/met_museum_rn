import {StyleSheet} from 'react-native';

export const Layouts = StyleSheet.create({
  textInputWrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    fontSize: 24,
    paddingLeft: 12,
    paddingRight: 8,
    borderRadius: 8,
    marginHorizontal: 12,
    marginVertical: 4,
  },
});
