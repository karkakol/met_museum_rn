import {StyleSheet} from 'react-native';

export const Layouts = StyleSheet.create({
  textInputWrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    fontSize: 24,
    paddingHorizontal: 12,
    borderRadius: 8,
    marginHorizontal: 12,
    marginVertical: 4,
  },
});
